# Habilita a API (autenticação virá do docker-compose)
api: yes

# Habilita o listener do WebRTC
webrtcAddress: :8889

# Permite que o frontend (de qualquer origem) se conecte
webrtcAllowOrigin: '*'

# Paths serão adicionados pela API
paths: {}